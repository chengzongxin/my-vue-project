// import Vue from 'vue'
// import Fetch from '@to8to/fetch'
// import { TuToast, TuImage, TuModal, TuButton } from '@to8to/tu-view-mobile'
// import main from './main.vue'
// import '@/utils/seoStatisDoMain'

// Vue.use(TuToast)
// Vue.use(TuImage)
// Vue.use(TuModal)
// Vue.use(TuButton)

// if (process.env.NODE_ENV && process.env.NODE_ENV === 'development') {
//   import('vconsole').then(({ default: VConsole }) => {
//     // eslint-disable-next-line
//     new VConsole()
//   })
// }
// Vue.prototype.fetch = new Fetch({
//   headers: {
//     'Content-Type': 'application/x-www-form-urlencoded',
//   },
// })

// new Vue({
//   render: (h) => h(main),
// }).$mount('#root')
